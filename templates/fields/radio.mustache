{{#options.options}}
    <div class="{{class}}">
        <input class="form-check-input" type="radio" value="{{value}}" name="{{elementname}}"
               {{#selected}}checked="true"{{/selected}} >
        <label class="form-check-label">{{name}}</label>
    </div>
{{/options.options}}